# OpenFlow

节点式 AI 工作流画布编辑器：支持多个项目，每个项目是一块画布，可在画布上添加节点
（节点列表按输出形态分三类：**文本** Prompt 节点 / **图像** 生成节点 / **视频** 生成节点）并用连线把
节点连接起来。**选中多个节点右键**可对其**分组**（建一个容器节点包住、一起移动/改名/取消分组）或**整理**（网格排列）。**上传资源现在只有一种方式**：从桌面**拖入图像/音频/视频文件到画布空白处**，经 /api/upload 上传后在落点生成 **素材节点**（图像素材 / 音频素材 / 视频素材，纯「源」节点；视频走媒体上传端点），
再**连线**到下游节点作输入——图像素材作输入图、音频素材作视频 audio_list（不再支持「拖到已有节点上追加输入」，也不在 Inspector 里上传/手填 URL——资源一律先落素材节点、经连线喂给下游。）
图像类的 **Image 2(gpt-image-2)** 与 **Nano Banana(nano-banana)**、视频类的 **Seedance(seedance)** / **可灵(kling)** / **MiniMax-H3** 均已接入真实生成
（经后端代理调 AIGC 接口；prompt 取自上游 Prompt 节点，节点上可调各自参数，输入图/音频均来自连线（上游图像素材·生成结果 / 音频素材），运行后展示结果图/视频）。
**统一资源端口 + Prompt 节点 @ 引用**：图像/视频生成节点只有一个**统一资源输入端点 `res`**（图/音/视素材与上游生成结果都连这一个口，可连任意多条，无 Add Input 按钮；**框选多个资源节点后可一并连线**——两条路：① 从其中一个节点的输出端点拖线到目标，其余选中的合法资源自动跟上；② 选区包围盒中心浮出的**批量连线按钮**（≥2 个资源节点时出现）直接拖到目标节点卡片/端点，或拖到空白处弹菜单建节点再全连上。两者都按节点创建序、类型不符的静默跳过；
图像节点的 res 只收图像源、视频节点收图/音/视；旧编号端点 image-N/audio-N/video-N 连线**载入时自动归并**到 res 且保持旧端点序；**例外**：视频「首尾帧」变体保留 First/Last 两个专用图像端点定首/尾帧）。
Prompt 文本框输入 `@` 弹候选菜单（列出与本 Prompt 下游图像/视频节点连线接入的资源，含 Prompt 链递归；IME 组词不误触、email 场景有边界判定），选中插入可读 token `@[显示名]`
（素材=文件名 / 生成结果=「label·结果N」，同名消歧；**画布上 token 带按资源类型配色的高亮 tag**——Textarea 后方 overlay 层画底色药丸，**悬停 tag 弹资源缩略图预览**）、身份存 `PromptNodeData.mentions`。
**发送语义（@ 调用）**：构建图像/视频请求时按资源类型各自判断——prompt 里 @ 到该类资源 → **只发被 @ 的**（@ 出现序即列表序）；一个都没 @ → 退化为全发连线资源（连线序）；
frames 变体的 image_list 始终取 First/Last 专用端点前 2 张不受 @ 筛选。prompt 中 token 替换为 `<<<image_N>>>`/`<<<audio_N>>>`/`<<<video_N>>>` 占位符（N=资源在**最终实发**列表的 1 基序号；
改名/断线/被筛掉/被裁掉等悬空引用原样保留）。
调用方署名 **req_from** 为全局设置（首次打开网站若未填则强制填写，存后端单例 settings；图像/视频生成与图片上传统一由后端注入）。
工作区右侧有**画布 Agent 聊天面板**（**默认收起**，只在画布右下角留悬浮按钮作入口；展开/收起偏好存 localStorage）：用自然语言说想法/想要的画面，后端调 OpenAI 兼容 LLM 产出
`{reply, actions}` 计划，前端把每个 action 落成一组「Prompt 节点（写好提示词）→ 图像节点（Agent 选模型）」并连线、建生图任务，
结果沿用节点自身的 taskId 重连轮询展示；Agent 的接口地址/API Key/模型名在设置面板配置。
**播客 TTS（火山）节点**（音频类）：节点内置双人对话脚本（每行「角色名: 台词」，支持 [轻笑] 等方括号表演指令透传），右侧 Inspector 配两个角色的火山音色 ID + 语速；运行走异步任务，后端逐行调火山单向流式 TTS（seed-tts-2.0）合成 pcm、行间插静音拼成整期 WAV 落盘（<数据目录>/files，经 GET /api/files/:name 同源服务），节点 <audio> 播放 + 下载；鉴权用设置里的火山语音 API Key（写入-only，env 回退 VOLC_TTS_API_KEY），不需要 req_from；终端节点（无连接点）。**脚本切割 + 脚本分镜节点**（工具类，成对使用）：**切割节点**粘贴整篇双人播客脚本原文（标题/小节标题行自动跳过、无前缀长行并入上一句），按**节点上可选的语速档位**（4/5/6/7/8 字/秒下拉，默认 6，随节点持久化）切成 **4~15s** 的段（seedance 2.0 单段上限；句读边界贪心装箱、超长句按逗号再切、短尾并回——语速同时决定单段字数上限与各段估算时长），点「切割」自动**新建（或更新）下游已连线的分镜节点**；**语速只作用于切割节点**（分镜节点只接收切好的段落，其行内改脚本重估/粘贴切分/Excel 导入缺时长仍按默认 6 字/秒）；**分镜节点**是 Excel 式表格——表头 序号/发言人/脚本（可编辑，改完自动重估时长）/时长（可编辑）/生成/prompt，表格三入口（上游切割节点写入 / 节点上「粘贴脚本切分」/ **「从 Excel 粘贴」TSV 导入**——列 [序号,] 发言人, 脚本 [, 时长]，表头行与序号列自动跳过、带引号多行单元格还原、发言人自动采用为角色名），**「复制表格」**把整表（含表头与 prompt 列）以 TSV 写入剪贴板直接粘回 Excel；点「生成」逐段并发调 Agent LLM（`POST /api/agent/expand`，复用 Agent 端点配置，限并发 3、单段可重跑、结果随项目持久化；模板占位符 `{{line}}`=「角色名: 段文本」、`{{duration}}` 可选=该段秒数），点「落成节点」批量创建 N 组「Prompt→Seedance 参考图变体」节点对并连线（**C 列时长写进各视频节点的时长参数**，不自动运行）；分镜节点左侧 5 个端点（分镜表 + 每角色一张人像参考图 + 一条音色参考音频；**按角色分组排布**——同一角色的参考图与音色相邻、两角色之间空出一个槽位），落成时按说话人把图和音自动连给对应视频节点，prompt 里的 @Image1/@Audio1 引用自动转成 `<<<image_1>>>`/`<<<audio_1>>>` 实发占位符（每节点各 1 条，序号正好对上）。顶栏还有 **Prompt 预设** 库（全局共享的常用/System 提示词，Prompt 节点卡片可一键选用或「存为预设」；存后端 `prompt_presets` 表、首次启动播种默认 3 条）。
图像/视频结果卡片有**下载**按钮（经后端 `/api/download` 同源代理跨域拉流、按响应类型自动补扩展名、可自定义文件名）。**前后端架构**：
数据存后端 SQLite，AIGC 调用经后端代理（绕开 CORS）。单用户、无鉴权。

## 仓库结构（pnpm workspaces monorepo）

```
apps/web/        前端（Vite + React + React Flow + shadcn/ui），包名 @openflow/web
apps/server/     后端（Hono + better-sqlite3），包名 @openflow/server
apps/desktop/    桌面端外壳（Electron，打包 mac/win 安装包），包名 @openflow/desktop
packages/shared/ 前后端共享的纯 TS 类型/常量，包名 @openflow/shared
```

## 常用命令（在仓库根运行）

```bash
pnpm dev:all       # 同时起前端(5173) + 后端(8787)（concurrently）
pnpm dev           # 只起前端（= --filter @openflow/web dev）
pnpm server        # 只起后端（tsx watch）
pnpm build         # pnpm -r build（全包）
pnpm typecheck     # pnpm -r typecheck
pnpm test          # pnpm -r test（当前只有后端 node:test 单测：AIGC 历史认领 / 响应解析）
pnpm lint          # pnpm -r lint
pnpm format        # Prettier 格式化 apps/*/src 与 packages/*/src（不含 components/ui）

# 桌面端（Electron）打包 —— 详见「桌面端打包」章节
pnpm --filter @openflow/desktop dist:mac   # 产 mac dmg/zip（arm64 + x64/Intel，未签名）到 apps/desktop/release
pnpm --filter @openflow/desktop dist:win   # 产 win nsis 安装包（x64，未签名；在 mac 上交叉构建：先拉 win 预编译 better-sqlite3 再打包）
pnpm --filter @openflow/desktop dev        # Electron 开发（自动切 electron ABI + 连 Vite dev server:5173）
```

## 目录结构

```
packages/shared/src/index.ts   ProjectDTO(含 pinned 置顶标记) / SettingsDTO(defaultReqFrom + aigcEndpoint/uploadEndpoint/uploadMediaEndpoint/aigcHistoryEndpoint + agentEndpoint/agentApiStyle('responses'|'chat'|''，''=未选择回退默认)/agentApiKey/agentModel) / SaveSettingsBody(defaultReqFrom 必填 + 其余可选，省略保持原值) / GenImageBody / GenVideoBody(含 projectId/nodeId + audios → audio_list + 模型特有项 generateAudio/sound/qualityMode/multiShot/shots/watermark，后端按 model 取舍) / VideoShot{prompt,duration}(可灵分镜；index 由后端按数组序生成) / TaskDTO(id/projectId/nodeId/kind/status/result/error + upstreamId(上游 request_id/历史记录 id，认领结果的钥匙兼防抢占锁)/rawResponse(上游原始响应截断，失败现场)/recoverable(失败是否值得去历史重拉)) + TaskKind('image'|'video'|'podcast') + 播客契约(PodcastRole{name,voiceId} / GenPodcastBody{projectId/nodeId/script/roles×2 + 可调项 speechRate/sampleRate/loudnessRate/pitch/lineGapMs(本地句间静音)/filterParenthesis/disableMarkdownFilter/disableEmojiFilter/explicitLanguage/contextText(语音指令，UI 已隐藏字段保留)/aigcWatermark/aigcMetadata{enable+四元信息字段}，全部可省略走默认}；podcast 任务 result=[音频 URL, 计费字数(各句 usage.text_words 合计)]) + SettingsDTO.volcTtsApiKey(写入-only，GET 恒空串 + hasVolcTtsApiKey) + TaskStatus + CreateTaskResponse + AgentApiStyle('responses'|'chat'，Agent LLM 的线格式) + 画布 Agent 契约(AgentMessage / AgentChatBody / AgentImageAction{prompt,model,title?} / AgentChatResponse{reply,actions} + 连接测试 AgentTestBody{endpoint?,apiKey?,model?,apiStyle?}·AgentTestResponse{ok,model,apiStyle,latencyMs} + 动态模型列表 AgentModelsBody{endpoint?,apiKey?}·AgentModelsResponse{models} + 脚本分镜逐行扩写 AgentExpandBody{template,line}·AgentExpandResponse{prompt}) + Prompt 预设契约(PromptPresetCategory 'common'|'system' / PromptPresetDTO{id,title,content,category,时间戳} / SavePromptPresetBody{title 必填 + content/category 可选})
apps/server/src/
  index.ts                     独立开发入口（tsx，固定 8787）：调 startServer()，数据走源码相对目录；并 re-export createApp/startServer
  app.ts                       createApp({staticDir?})：挂 /api 路由（含 /api/tasks）+ import './task-store'（启动即对账中断任务）+ 可选在根路径托管前端 SPA（供 Electron 生产用；HashRouter 故非 /api 路径统一回退 index.html，含目录穿越防护）
  server.ts                    startServer({port?,dataDir?,staticDir?})：先设 OPENFLOW_DATA_DIR 再动态 import app（保证 db 读到注入目录）；绑 127.0.0.1，port=0 时取空闲端口，监听失败（端口被占）reject 供调用方回退，返回 {port, close}
  db.ts                        better-sqlite3 建库建表（projects / settings / tasks / prompt_presets）；projects 表列 id/name/nodes/edges/pinned(置顶 0|1)/updated_at（早期无 pinned 列 → ALTER 补默认 0）；数据目录 = env OPENFLOW_DATA_DIR（桌面端注入 userData）否则回退 apps/server/data/openflow.db（gitignore）；settings 表列 default_req_from + aigc/upload/upload_media/aigc_history_endpoint + agent_endpoint/api_style(接口协议，''=未选择)/api_key/model + agent_model_list(JSON 手动模型列表，默认 '[]')（按需 ALTER 迁移，旧 provider 列留存不读）；tasks 表(id/project_id/node_id/kind/status/params/result/error/upstream_id/raw_response/时间戳 + node 索引；早期无 upstream_id/raw_response 列 → ALTER 补默认 '')；prompt_presets 表(id/title/content/category/时间戳；早期无 category 列 → ALTER 补默认 'common')
  settings-store.ts            读写 settings 单行（defaultReqFrom + 四端点(AIGC 生成/图片上传/音视频上传/AIGC 历史查询) + agent 四项(端点/协议/Key/模型) + agentModelList 手动模型列表，存 JSON 字符串，读时 parse/去空去重、写时 stringify；**SQL 列序与 .run() 参数序是手工对齐的两处，加列必须同改**）；writeSettings(patch) 合并写：只覆盖出现的字段
  preset-store.ts              Prompt 预设持久层：SQLite 表 prompt_presets(id/title/content/category/created_at/updated_at)；listPresets(updated_at DESC)/createPreset/updatePreset/deletePreset + seedDefaultPresets()（首次启动仅表空时灌入 default-presets）
  default-presets.ts           分发预设默认内容数组（3 条：人物三视图+特写 / 人像写真 常用 Prompt + 资深提示词工程师 system prompt；数组顺序=列表顺序）
  agent.ts                     画布 Agent 的**业务语义层**（协议细节全在 llm.ts）：runAgentChat(messages, settings)（系统提示词要求严格 JSON 计划 + 容错解析——extractJsonObject 从首个 { 起做括号平衡扫描，容忍 ``` 围栏与 JSON 后追加的说明文字；非 JSON 时整段当 reply 且 actions 空）+ normalizeActions（无 prompt 丢弃、model 归一为 Image 2/Nano Banana、单轮上限 8 个动作）+ runAgentExpand(body, settings)（脚本分镜逐行扩写：模板 {{line}} 全部替换为台词行后作**唯一一条用户输入**单次调 LLM——刻意不带生图 SYSTEM_PROMPT，模板即完整指令，返回 {prompt} 纯文本不做 JSON 计划解析）+ runAgentConnectionTest（最小用量连接测试，**只看 HTTP 状态不解析内容**——推理模型在最小 token 上限下必然返回 incomplete，一解析就把正常网关误报成失败；返回 {model,apiStyle,latencyMs}）+ listAgentModels(override, settings)（GET /models 列可用模型 ID，与协议无关；空/失败抛可读错误）。四个入口统一为「resolveAgentConfig → resolveLlmUrl → build*RequestBody → requestLlmJson → readLlmText」五步
  llm.ts                       **LLM 接入层（两种线格式集中在此）**：normalizeAgentApiStyle(脏值/空→默认 responses) / resolveLlmUrl(endpoint,style)（**先剥** /responses·/chat/completions·/models 旧后缀**再补**当前协议后缀——切协议时不剥就会拼出 .../chat/completions/responses；用 new URL 只改 pathname 保住查询串如 Azure ?api-version=，非法 URL 退字符串拼接）/ resolveModelsUrl（两种后缀都剥回同一基址补 /models，故**切协议无需重取模型列表**）/ buildPlanRequestBody·buildExpandRequestBody·buildProbeRequestBody（两协议请求体：responses 走 instructions+input（扩写用裸字符串 input 且不发 instructions）、chat 走 messages；连接测试 responses 侧 **max_output_tokens:16**——该参数下限是 16，照搬 chat 的 max_tokens:1 会被 400）/ readLlmText(style,data)（统一取文本：chat 读 choices[0].message.content；responses 优先 output_text（**必须判 trim 非空**，网关会回空串）→ 回退遍历 output[] 取 type==='message' 的 output_text 分片、**跳过 reasoning 项**（推理模型把它排首位）；软失败 incomplete/refusal/failed 翻译成具体原因而非「返回为空」）/ parseModelList / extractLlmError / resolveAgentConfig(settings,override?,{requireModel?})（override→settings→env 三级回退；**缺失错误必须含「请在设置中填写」**，routes/agent.ts 靠该子串分流 400/502）/ requestLlmJson（唯一 fetch 出口：Bearer、超时、先 text 再 JSON、错误翻译）/ llmTextError
  llm.test.ts                  上述纯函数的 node:test 单测（23 例）：端点推导五类输入（基址/已带本协议后缀/带另一协议后缀的历史配置/带查询串/非法 URL）、两套请求体形状、readLlmText 的 reasoning 混排与 output_text 空串短路与三种软失败与「协议选错收到 chat 形状」、配置回退与「请在设置中填写」约定
  task-store.ts                异步生成任务持久化 + 进程内 runner：createTask/getTask/getLatestTaskForNode；startTask(不 await 地后台跑，图像/视频经 planFor 装配「请求体(指纹用) + 发送动作」两件套，reqFrom/端点运行时从设置解析不入库，完成回写 succeeded/failed)；**sendOrRecover**：上游 2xx 没带回 URL(EmptyResultError) 或请求被掐断(isNetworkError) 时**不判死**，先把 upstream_id/raw_response 落库再转 pollHistoryForResult 去历史认领结果，任务全程保持 running(上游明确失败则原样抛真实原因，不做无谓复查；没配历史端点则降级为报错并提示去设置里填)；reconcileInterruptedTasks()（模块加载时调一次：配了历史端点的图像/视频任务重新挂上复查续跑——seedance 合法要跑几十分钟，桌面端重启/dev 热重载都会撞上；其余才标 failed「任务因服务重启中断」）；refetchTask(id) 手动重拉(单次查历史，命中则改回 succeeded)
  provider.ts                  buildImagePayload()/buildVideoPayload()：构造请求体（**独立于发送导出**——失败后要靠同一份请求体去历史按指纹认领，两处各写一遍必然漂移）；buildVideoPayload **按 model_name 分三支**：seedance={resolution,duration,ratio?,generate_audio?} / kling={duration:字符串,sound:'on'|'off',mode:'std'|'pro',aspect_ratio,multi_shot,shot_type?}（多镜头时**整个不发 prompt**、改发顶层 multi_prompt；分镜全空则自动退回单镜头）/ MiniMax-H3={resolution,ratio,duration,'aigc-watermark'}（空的音视频列表不下发）；三套形状钉在 provider.test.ts 里比对文档示例；runImageGen()/runVideoGen()：POST AIGC 接口生成图像/视频（端点取入参 endpoint，空回退 env AIGC_ENDPOINT）→ GenOutcome{urls,requestId,rawText}；postAigc() 用 res.text() 再 JSON.parse（**不用 res.json().catch(()=>null)**——非 JSON 响应会连原文带状态码一起丢掉，错误退化成字面量 null）；readAigcResult() 按已知契约 `result:{content,status,error{code,message}}` 解析（契约不匹配才回退 extractLegacyUrls 深挖），extractError() **扫到第三层 result.error.message**（本网关错误在那儿，只扫顶层会把「输出音频涉及版权限制」这类真实原因整个吞掉），URL 提取带 exclude 剔除本次请求发出的输入 URL（防回显输入图被当成结果）；EmptyResultError（2xx 但无 URL 且上游没说失败＝可能还在生成，供 task-store 转历史复查）；uploadFiles(form,kind,endpointOverride?)：转发 multipart 到上传接口（kind=image 走图片端点，audio/video 走媒体端点；端点取 override，空回退 UPLOAD_ENDPOINT / UPLOAD_MEDIA_ENDPOINT）；resolveReqFrom()：把全局署名解析成最终 req_from（**为空即抛错、无兜底默认**——req_from 空时不允许发任何上游请求）
  aigc-history.ts              AIGC 历史任务复查（**「空 URL 也能找回结果」的核心**）：生成接口偶尔 2xx 却不带结果 URL（长连接被中间设备掐断等），但内容其实已生成并稍后落进网关历史——内部另一个平台「重启刷新就能在历史里找回 URL」即此。fetchHistory(GET {endpoint}?ip=&req_from=&sort=last_seen_desc&page=1&page_size=50 → {items,total}，每条含完整 request 与 response) / matchHistoryItem(认领本次任务的记录：① 有 request_id 走精确匹配 ② 否则按请求指纹 + 时间窗(提交时刻前推 5min 时钟容差) ③ 多条取最新、跳过已被别的任务认领的 claimKey 防互抢) / fingerprintFromPayload(**刻意不含 config**——网关会补 generate_audio/watermark 等我们没发的默认字段再落库，全等比对必然失配) / claimKeyOf(优先 response.request_id，退而记录 id) / pollHistoryForResult(5s 起 ×1.3 封顶 30s 退避轮询至终态：status=success 取 result.content、failed 抛 result.error.message 真实原因；总时长上限 30 分钟，env AIGC_HISTORY_DEADLINE_MS 可覆盖)；端点空则整套能力降级关闭，不报错
  provider.test.ts             buildVideoPayload 三套 config 形状的 node:test 单测（断言值对齐 docs/模型适配.md 的示例请求体：可灵 duration 是字符串、MiniMax 的水印键带连字符、多镜头整个不发 prompt——这类差异靠肉眼比对必然漂移）
  aigc-history.test.ts         上述纯函数的 node:test 单测（`pnpm --filter @openflow/server test`），夹具 __fixtures__/task-history.sample.json 是**脱敏后**的真实响应结构（host/req_from/签名 URL 全部替换过——仓库公开，勿回填真实数据）
  routes/projects.ts           /api/projects CRUD（nodes/edges 以 JSON 存；列表 ORDER BY pinned DESC, updated_at DESC 置顶优先；PUT 的 pinned 用 undefined 判断——取消置顶的 false 才写得进去，且**只切 pinned 时不刷新 updated_at**，避免置顶开关打乱「最近更新」次级排序）
  routes/settings.ts           GET /api/settings(回 defaultReqFrom + 三端点 + agent 配置 + agentModelList；**agentApiKey 不回明文**——恒空串 + hasAgentApiKey 标记) / PUT(defaultReqFrom 必填；其余省略保持原值、空串清空回退默认；agentModelList 传数组则整体覆盖 trim/去空/去重、含空数组=清空)
  routes/image.ts              POST /api/aigc(图像生成：校验 model/prompt/projectId/nodeId + req_from 空返回 400 拒发 → createTask/startTask 建任务、立刻返回 {taskId}；实际 AIGC 由 task-store runner 后台跑)
  routes/video.ts              POST /api/video(视频生成 seedance/kling/MiniMax-H3：同 image.ts 建任务返回 {taskId}，kind=video；**prompt 校验对可灵多镜头放行**——那时画面描述在 multi_prompt 里、顶层 prompt 本就为空，改校验分镜非空)
  routes/tasks.ts              GET /api/tasks/:id(轮询单任务，缺失 404) / GET /api/tasks?projectId=&nodeId=(取节点最近一次任务，重连兜底) / POST /api/tasks/:id/refetch(手动重拉：再去 AIGC 历史接口认领一次结果，拿不到回 502 + 可读原因)
  routes/upload.ts             POST /api/upload(文件上传代理：按 query kind 分流转发 multipart——图片(kind=image)→uploadEndpoint、音频/视频(kind=audio|video)→uploadMediaEndpoint（均从设置注入，空回退对应 env）；req_from 从全局设置注入，为空返回 400 拒发)
  routes/agent.ts              POST /api/agent/chat(画布 Agent 对话：过滤非法消息并截取最近 20 条历史 → runAgentChat 同步调 LLM 返 {reply,actions}；配置缺失 400、上游失败 502；本接口不建任务——画布动作与生图任务由前端执行) / POST /api/agent/test(最小用量连接测试；**apiStyle 也透传**——设置面板支持「切了协议还没保存就点测试」，不传就会测成旧协议) / POST /api/agent/models(动态获取模型列表：body 可带 endpoint/apiKey override，省略回退已存设置 → listAgentModels 返 {models}；配置缺失 400、上游/端点不支持 502) / POST /api/agent/expand(脚本分镜逐行扩写：{template,line} → runAgentExpand 返 {prompt}；前端逐行并发调用、单行可重试；缺参/配置缺失 400、上游失败 502；不建任务不落库)
  routes/podcast.ts            POST /api/podcast(播客音频生成：校验 script/roles×2 + 火山 Key 缺失 400 早失败 → createTask/startTask 建任务返回 {taskId}，kind=podcast；不需要 req_from)
  routes/update.ts             GET /api/update-check(检查更新：查 GitHub Releases 最新版返回 {latest,url,error?}，进程内缓存 30 分钟、?force=1 跳过；**刻意不做版本比较也不持有版本号**——当前版本的唯一权威来源是前端 APP_VERSION，后端再存一份就成了第三处双写；任何失败仍返 2xx + error，前端静默不打扰。发布仓库可用 env OPENFLOW_RELEASE_REPO 覆盖)
  routes/files.ts              GET /api/files/:name(后端生成文件的同源服务：<数据目录>/files 下按文件名取，防目录穿越；播客 WAV 经此播放/下载)
  volc-tts.ts                  播客合成核心：resolveVolcTts(设置 volcTtsApiKey 优先→env VOLC_TTS_API_KEY，缺失抛可读错误；端点 env VOLC_TTS_ENDPOINT 回退官方地址) / parsePodcastScript(每行「角色名: 台词」中英冒号均可、长角色名优先匹配、无前缀行并入上一句、首句无法识别抛错) / runPodcastGen(逐行串行调火山单向流式 HTTP 拿 base64 pcm——流式 JSON 包括号平衡解析、code 0/20000000(结束包) 之外才抛错，行间插句间静音(lineGapMs 默认 300ms)拼单声道 16bit WAV 落盘，返回 /api/files/xxx.wav)；每句请求带 audio_params{sample_rate/speech_rate/loudness_rate} + additions(JSON 字符串，仅非默认项：括号过滤/markdown/emoji/explicit_language) + post_process.pitch(非 0 才发) + context_texts(语音指令) + additions.aigc_watermark/aigc_metadata(meta 水印不支持 pcm——enable 时每句改按 wav 请求、wavToPcm 解出 data 块再拼接)；每句结束包 usage.text_words 累加随结果返回
  routes/prompt-presets.ts     Prompt 预设 CRUD：GET /api/prompt-presets(列表) / POST(新建) / PUT /:id(更新) / DELETE /:id；parseBody 校验(title trim 非空、content 允许空、category 归一为 'common'|'system')
  routes/download.ts           GET /api/download?url=&kind=&name=(下载同源代理：fetch 跨域结果资源流式回传 + 按 Content-Type/URL 后缀/kind 推断扩展名 + 清洗文件名去路径分隔/保留字 + Content-Disposition 用 RFC 5987 filename* 兼容中文)
apps/web/src/
  main.tsx                     入口：先 migrateLocalStorage() 迁移旧数据，再 load store，最后渲染（HashRouter）
  App.tsx                      用 ReqFromGate 包裹路由（/ → 首页，/project/:id → 工作区，* → 回首页）
  store/useFlowStore.ts        Zustand（无 persist）：启动 loadProjects() 拉后端（载入时 normalizeResourceEdges 把旧编号端点资源连线归并到统一端点 res）；增删改调 API；onConnect 除 addEdge 起点边外再调 collectMultiConnectEdges 补上「其余选中资源」的连线，并把本次新增的边按节点创建序排序（addEdge 把起点边追加在末尾，不排的话「从哪个节点起拖」会决定它排第一、进而影响没写 @ 时的实发 image_list 顺序）；addConnectedNode 从资源源拉线建图像/视频节点时自动连到 res 端点；setProjectPinned(id,pinned) 置顶/取消置顶（照 renameProject 乐观更新 + PUT {pinned}；防抖存盘只 PUT name/nodes/edges 故不会冲掉 pinned）；
                               connectSelectedResourcesTo(targetNodeId,targetHandle) / addNodeWithSelectedResources({type,model,position,videoVariant})：批量连线按钮的两个落点动作（连到已有节点的端点 / 在落点建节点再把选中资源全连到其 res）；
                               groupSelectedNodes/ungroupNode/arrangeSelectedNodes：选中多节点分组（建 group 容器 + 子节点设 parentId/extent、坐标转相对、容器排在子节点前）/ 取消分组（detachChildren 释放子节点转绝对 + 删容器）/ 整理（computeGridLayout 网格排列）；onNodesChange 拦截 group 删除时先 detachChildren 释放子节点（防 parentId 悬空错位）；
                               画布编辑本地即时更新 + 防抖(500ms) PUT 保存激活项目；homeView 存 localStorage；
                               addAssetNode(kind,position)/removeNode(id) 供拖拽建/撤素材节点；duplicateNode(id) 在原节点正右侧（让开 nodeSize 整宽 + ARRANGE_GAP，不重叠）复制一份副本（换新 id、保留参数与结果快照、清 taskId/运行态、选中态转副本；**输入连线一并复制**——同上游、同端点、按原数组序追加，复制出来即可直接跑；**输出连线不复制**，否则等于凭空给下游多塞一份输入；group 容器跳过）；addAgentGeneration({prompt,model,title?}) 供 Agent 在现有内容下方建「Prompt→图像」节点对并连线、返回两节点 id（摆放按 measured.height/兜底高度找底部空位）；updateNodeDataInProject(projectId,nodeId,data) 显式按项目写节点 data（异步回调防「等待期间切走画布」丢写）；**脚本分镜四 action**：setStoryboardItems(projectId,nodeId,items,running) 整表重建 / patchStoryboardItem(projectId,nodeId,index,patch)（**set 时刻读最新 items 改一格**——并发 worker 各持过期闭包整表回写会互相覆盖）/ addStoryboardShots({storyboardNodeId,shots,model?})（落成节点：一次 patchActive 建 N 组「Prompt→Seedance(reference)」+ 连线，prompt 先过 normalizeShotPrompt 归一 @ 引用，按说话人从分镜节点角色端点反查参考图/音色素材各连一条到视频节点 res，**C 列时长写进各视频节点 data.duration**，行高 460 纵向排布，不建任务不自动跑）/ splitScriptToStoryboard({splitterNodeId,roleAName,roleBName,items})（切割节点「切割」：下游已连分镜节点则整表更新（同步角色名、D 列清空），否则在切割节点右侧新建并连 segments 端点——重切不堆节点）；载入时复位 asset 的 uploading 与 image/video 的 running/error、storyboard 的 running 与 items 中 pending/running→idle（但**保留 taskId/result 与 done 行的 prompt**，供节点重连未完成任务）
  store/useSettingsStore.ts    Zustand（无 persist）：loadSettings() 拉 defaultReqFrom + 四端点(含 aigcHistoryEndpoint) + agent 四项(端点/协议 agentApiStyle/Key 标记/模型) + agentModelList（手动模型列表，持久）；saveSettings(部分字段) PUT 后回拉（并使动态模型列表失效重取）；saveReqFrom() 为其薄封装（其余字段由后端合并保留，供 ReqFromGate）；另持有动态模型列表 agentModels/agentModelsLoading/agentModelsLoaded/agentModelsError + loadAgentModels(override?)（调 listAgentModelsApi，失败不抛、置空列表 + 存 error 供 UI 回退手填；设置面板与 Any LLM 节点共用这一份）
  store/useAgentStore.ts       Zustand（无 persist）：画布 Agent 会话（conversations 按 projectId 分、仅存内存刷新即清）+ sending 态 + panelOpen（存 localStorage openflow-agent-panel；**默认 false 收起**——判 '1' 而非 !== '0'，没存过偏好时才落到默认）；send() = agentChatApi 拿计划 → executeAgentActions 落画布 → 把 reply/执行结果（成功组数 okCount、失败信息 actionErrors）追加进会话；有动作的 assistant 轮以 llmContent 存完整 JSON 计划回灌 LLM 历史（供「照上一张改」类追问拿到 prompt 全文，展示仍用短 reply）；错误提示条不进 LLM 历史，且历史构建时合并连续同角色消息（严格网关要求角色交替）
  store/useThemeStore.ts       Zustand：主题偏好 mode(light/dark/system) 存 localStorage(openflow-theme)；resolved 为实际明暗（system 已按 prefers-color-scheme 解析，供 React Flow colorMode）；创建即给 <html> 挂/摘 .dark 并监听系统明暗变化（index.html 另有防白闪内联脚本在首帧前先挂类）
  store/usePromptPresetStore.ts Zustand（无 persist）：Prompt 预设全局库（presets + loaded + loadPresets/addPreset/editPreset/removePreset）；走 /api/prompt-presets CRUD，乐观更新，新建/编辑后置顶（updated_at DESC 对齐后端）
  lib/api.ts                   /api/* fetch 封装（项目 CRUD / 设置 get·save / 图像·视频·播客异步任务 createImageTaskApi·createVideoTaskApi·createPodcastTaskApi 建任务返 taskId、getTaskApi 轮询、refetchTaskApi 手动重拉(POST /tasks/:id/refetch)、getLatestTaskForNodeApi 按节点重连 / agentChatApi(Agent 对话，同步返 {reply,actions}) / agentExpandApi(body,signal?)(脚本分镜逐行扩写，POST /agent/expand 返 prompt 纯文本；signal 供节点卸载中止) / testAgentConnectionApi(连接测试) / listAgentModelsApi(动态取模型列表，POST /agent/models) / 文件上传 uploadFilesApi(files,kind)——图片默认走 /api/upload，音频 kind=audio 走 /api/upload-media / Prompt 预设 CRUD listPromptPresetsApi·createPromptPresetApi·updatePromptPresetApi·deletePromptPresetApi）
  lib/requestBody.ts           生成请求体的**单一来源**（视频侧 buildVideoUpstream **逐字镜像 provider.ts buildVideoPayload 的三个模型分支**，改一处必须同步另一处）：buildImageRequest/buildVideoRequest/buildPodcastRequest 构造「点击生成时发送的请求 JSON」（收集上游 Prompt/图像/音频/视频 + 按模型整合参数；图像/视频请求先经 collectMentionedRefs 做 @ 筛选——某类资源被 @ 到则实发列表只含被 @ 的（@ 序）、没 @ 则全发连线资源（连线序），frames 变体 image_list 固定取 First/Last 专用端点前 2 张不受筛选；再经 applyMentions 把 @[名] 替换成 <<<kind_N>>> 占位符——N 按最终实发列表算）；节点 handleRun 与 Inspector「请求预览」共用，保证预览=实发不漂移
  lib/agentExecutor.ts         executeAgentActions(projectId, actions)：把 Agent 计划落到画布——逐动作 addAgentGeneration 建节点连线 → createImageTaskApi 建任务（参数与 ImageNode 手动运行同构，按模型分组）→ updateNodeData 写 taskId（轮询/展示由 ImageNode 的 taskId 重连 effect 接管，节点组件零改动）；等待期间画布切走则跳过该动作；建任务失败把错误写到节点 data 内联展示
  lib/taskPolling.ts           pollTask(taskId,{onUpdate?,signal})：递归 setTimeout 轮询任务至终态（起 1500ms×1.3 退避封顶 5000ms；瞬时错误重试；signal abort 可取消）
  lib/graph.ts                 连线采集：collectUpstreamPrompt（上游 prompt 文本；prompt 有左侧输入→递归并入其上游文本实现 Prompt 链，visited 防环；opts.mentionsOut 出参沿途收集各 prompt 节点的 @ 引用映射）/ collectUpstreamImageRefs/AudioRefs/VideoRefs（带来源身份 UpstreamRef{url,nodeId,kind,resultIndex?,label,fileName?,handle?} 的采集，供 @ 引用与 frames 专用端点区分；图像=上游 image 结果+图像素材、音频=音频素材、视频=video 生成结果+视频素材）/ collectMentionCandidates（@ 菜单候选：从 Prompt 沿下游遍历（prompt 链递归、visited 防环），并入下游图像节点的图像 refs、视频节点的图/音/视三类 refs，按身份键去重 + 显示名消歧）/ findMentionResource(project,ref)（按 @ 引用身份 nodeId+kind+resultIndex 直查源节点拿资源，供 tag 悬停预览；刻意不复用 collectMentionCandidates 那套 O(E·N) 图遍历——mousemove 路径上太贵，且悬空引用也该能预览到资源本体） / RES_INPUT_HANDLE='res'（图像/视频节点的统一资源输入端点）+ normalizeResourceEdges（载入时把旧编号端点/空 handle 的资源连线归并到 res：按同目标同类型的旧排序键（端点号在前连线序在后）重排子组保证采集次序不变；frames 变体 First/Last 保留；无需归并原样返回）；端点 id 前缀 image-/audio-/video-（imageInputHandleId/audioInputHandleId/videoInputHandleId，现仅视频 frames 变体 First/Last 与旧数据归并使用）+ role-image-/role-audio-（storyboardRoleImageHandleId/storyboardRoleAudioHandleId(roleIndex)，脚本分镜的角色参考图/音色参考端点；专用前缀与生成节点端点体系隔离，normalizeResourceEdges 不碰它）+ STORYBOARD_SEGMENTS_HANDLE='segments'（分镜表端点，脚本切割节点的输出连这里——连线是组织性的，表格数据由切割动作直接写入）
  lib/mentions.ts              @ 引用 token 纯函数：MENTION_TOKEN_RE(@\[名\]) / mentionToken / sanitizeMentionName(剔方括号/控制字符) / uniqueMentionName(同名追加 " (2)") / collectMentionedRefs(按 @ 出现序收集「被 @ 且仍连线」的资源，身份去重分类型返回——「只发被 @ 的」筛选语义的数据源，悬空不计) / applyMentions(构建期把可解析 token 替换为 <<<kind_N>>>，N=URL 在实发列表 1 基首现位置；mention/ref/列表任一查不到→原样保留) / mentionTokenEndingAt(找「结束位置恰为光标」且已登记的 token，供退格整体删除；未登记的不认) / pruneMentions(提交文本时清掉 token 已消失的映射，悬空映射保留)
  lib/multiConnect.ts          框选多资源「一并连线」纯函数（三个导出，共用同一套「资源节点」口径：sourceKind 为 image/audio/video，即素材 + 图像/视频生成节点）：collectMultiConnectEdges(nodes,edges,connection) 供 useFlowStore.onConnect——仅当拖拽起点节点处于选中态才生效（否则是普通单连），把其余选中的资源节点按 nodes 数组序（=创建序，没写 @ 时即实发列表序）连到同一目标端点；collectSelectedResourceEdges(nodes,edges,target,targetHandle) 供批量连线按钮——没有「起点那根线」，选中项一视同仁全连（目标自身在选区则跳过）；selectedResourceNodes(nodes) 供按钮判断显隐 + 算包围盒。三者都经 isValidTypedConnection 过滤不合法的（如音频→只收图像的图像节点）+ 跳过已存在的同源同端点连线，静默跳过不报错
  lib/version.ts               isNewerVersion(latest,current)（只比数字段、忽略 v 前缀、正式版>预发布；不引 semver）+ isDesktop()（读 preload 注入的 window.openflow.desktop，Web 版不显示更新入口）
  lib/floating.ts              浮层定位纯函数 placeFloating(anchor,{width,maxHeight,side,align,gap})：首选方向放不下就翻转、两侧都不足取较大一侧并压缩 maxHeight、水平 clamp 防溢出；side='top' 时返回 bottom 而非 top（内容比 maxHeight 矮时才不会与锚点脱开）。@ 菜单与 tag 悬停预览共用
  lib/handleTypes.ts           端点数据类型↔配色 + 连接校验：HANDLE_COLORS（文本粉 / 图像绿 / 音频蓝 / 视频玫红）+ sourceKind/targetAccepts(返回接受类型数组或 null 不限)/edgeColorForSource/isValidTypedConnection（编号端点各接对应类型源、脚本分镜 role-image-N/role-audio-N 端点各接图像/音频源、segments 端点接文本源（脚本切割节点输出算文本）、文本端点只接文本源、统一资源端点 res 按节点类型收——视频节点收图/音/视、图像节点只收图、**其余类型压根没有 res 端点故一律不收**（批量连线按钮会拿 res 试连任意落点节点），供 FlowCanvas 连线校验与着色）
  lib/migrate.ts               首次启动把旧 localStorage（openflow-store）项目数据一次性导入后端，打 openflow-migrated 标记
  lib/types.ts                 React Flow 强类型节点（FlowNode = Prompt/Image/Video/Podcast/Asset/Group/Splitter/Storyboard；SplitterNodeData{label,script,roleAName,roleBName,charsPerSecond?(切分语速字/秒，旧数据缺失回退默认 6)}；StoryboardNodeData{label,script,template,roleAName,roleBName,items?,running?}（items=分镜表行 StoryboardItem{text(B 列段文本无前缀),roleIndex(A 列),duration?(C 列秒数),prompt?(D 列),status,error?}，status='idle|pending|running|done|error'，pending/running 载入复位；早期 line 字段载入时迁移为 text）；PromptNodeData{label,text,mentions?}（mentions=@ 引用身份映射表 PromptMentionRef{name,nodeId,kind,resultIndex?}，name 为插入时显示名快照，旧数据 undefined 兜底）；AssetNodeData{kind:image|audio|video,url,fileName,uploading,error}；GroupNodeData{label}(分组容器)；Project）；图像/视频节点 data 另含 errorRecoverable（上次失败是否值得去 AIGC 历史重拉，供 TaskFailurePanel 显隐「重新拉取结果」）与 imagesText/audiosText（旧手填 URL，兼容保留、无新增 UI）
  lib/layout.ts                节点布局纯计算（供分组/整理复用）：nodeSize(measured→width/height→按类型兜底) / computeBoundingBox(绝对包围盒) / computeGridLayout(按位置排序后 ceil(√n) 列的等间距网格) / detachChildren(子节点相对坐标转绝对、清 parentId/extent) + GROUP_PADDING/ARRANGE_GAP
  lib/nodeCatalog.ts           图像/视频预置模型 + 模型→AIGC model_name 映射(IMAGE_API_MODEL/imageApiModel、VIDEO_API_MODEL/videoApiModel：Seedance→seedance / Kling→kling / MiniMax→MiniMax-H3) + 各模型可调项选项(图像尺寸 19 档含 auto/质量/张数、Nano version/宽高比/尺寸) + **视频模型能力表 videoModelSpec(model, version)**（把三家差异集中一处：可选分辨率·宽高比·时长范围·是否支持自动时长(-1)·特性开关 features；seedance 2.5(doubao-seedance-2-5-260628) 是 480p/720p + 4~30s + generate_audio + 首尾帧强制 adaptive，旧 seedance 仍 480/720/1080p + 4~15s；kling 无 resolution 改用 std/pro 质量档 + sound + multi_shot，时长 3~15s；MiniMax 768P/2K + aigc-watermark，4~15s）+ 配套 videoVersionOptions/videoDefaultVersion/videoHasFeature/videoModeFor(参考侧 MiniMax=reference_frame，其余=reference_image)/videoVariantLabel(参考图·关键帧·参考帧)/videoAcceptsRefs(可灵只吃图、MiniMax 首尾帧只吃图) 与 normalizeVideoResolution·Ratio·Duration（超范围旧值统一回退，**面板与实发请求共用这套归一化**）+ mergeModelOptions(manual, fetched, current)（把手动列表 ∪ 动态获取合并成下拉候选：去空/去重/排序 + 当前已选值置顶；供设置面板 Agent 模型名下拉）+ 脚本切割/分镜常量（STORYBOARD_CHARS_PER_SECOND=6 默认语速、STORYBOARD_SPEED_OPTIONS 语速档位 4~8 字/秒 + normalizeSplitSpeed(旧数据/非档位值回退默认)、STORYBOARD_SEG_MIN/MAX_SECONDS=4/15、STORYBOARD_CONCURRENCY）+ 配色文案（节点与菜单共用，含素材节点 ASSET_NODE_META：图像=琥珀 / 音频=天蓝 / 视频=玫红）
  lib/modelCapabilities.ts     modelCapabilities(name)：按模型名正则**启发式推断**支持的能力（思考/图像/音频/视频理解，常见家族 gpt-4o/gemini/claude/o系列/deepseek/qwen-omni 等，未知模型全 false）+ hasAnyCapability + MODEL_CAPABILITY_LABELS/ORDER（供 Model 下拉展示能力图标；规则近似、易维护）
  lib/nodeMenu.ts              NODE_GROUPS：画布右键菜单点选建节点的分组清单（**建节点入口只有两个**：画布空白右键新建、端点拉线松开在空白处建节点+连线，二者都走这份清单）（按文本/图像/视频/音频/工具分组，含类型/模型/图标/videoVariant；视频类由 VIDEO_MODELS × 两变体展开成 6 项，变体叫法取 videoVariantLabel；工具组含脚本切割 + 脚本分镜节点）
  lib/storyboard.ts            脚本切割/分镜纯函数：splitScriptTurns(script,roleNames)（按「角色名: 台词」拆说话人回合，语义对齐 server 侧 parsePodcastScript——中英冒号/长角色名优先；标题容错：首个说话人行之前的行跳过、文中 ≤20 字无前缀短行=小节标题跳过、长行=续行并入上一句）/ splitTurnIntoSegments(text,charsPerSecond?)（回合按语速切段：句末标点切句 → 贪心装箱 ≤15s×语速 念出字（默认 6 字/s=90 字）→ 超长句按逗号再切（极端硬切兜底）→ <4s×语速 字的短尾并回前段防发呆）/ countSpokenChars（去空白标点后的字数）/ estimateSegmentDuration(text,charsPerSecond?)（字数/语速 四舍五入夹 4~15）/ buildItems(script,roleNames,charsPerSecond?)（脚本→分镜表行，切割节点与分镜节点「粘贴切分」双入口共用；**语速参数三处均可省略、省略即默认 6**——只有切割节点传自己配置的档位，分镜节点调用点零改动）/ itemsToTsv（分镜表→TSV 含表头，多行/含 Tab 字段按 Excel 约定双引号包裹）/ parseItemsTsv（Excel 粘贴的 TSV→分镜表行：带引号字段解析、表头行与纯数字序号列跳过、发言人去重 ≤2 与已配角色对不上则按出现序采用、时长缺省按语速估算）/ normalizeShotPrompt（@ImageN/@AudioN/@VideoN 字面引用 → <<<kind_N>>> 实发占位符，大小写不敏感；落成节点前调用）
  lib/storyboardTemplate.ts    STORYBOARD_TEMPLATE_DEFAULT：分镜节点内置默认 prompt 模板（固定指令 + 参考 prompt 范例 + {{line}} 台词槽；产出保留 @Image1/@Audio1 引用供落成时归一）
  lib/id.ts                    newId(prefix)：生成简短唯一 id（项目/节点/连线用）
  lib/appMeta.ts               应用元信息常量：APP_NAME='Open Flow' / APP_VERSION（工作区顶栏 logo 展示；与 apps/desktop/package.json 的打包版本保持一致，发版时两处一起改）
  lib/utils.ts                 cn()：clsx + tailwind-merge 合并去重类名（shadcn 约定）
  hooks/useSpacePanGuard.ts    空格平移守卫（FlowCanvas 调用）：焦点残留在节点输入框时，指针停在画布空白处按住空格会被输入框吃成一串空格、且 React Flow 因 isInputDOMNode 判定不激活平移。命中「焦点在节点内输入框 + 指针在空白处 + 已停手 700ms 或长按重复」三条时吞掉该空格 + 输入框失焦 + 补发一次 keydown 让平移立刻生效；组词中/带修饰键/指针停在节点上一律放行（正常打空格）
  hooks/useResizableWidth.ts   右侧停靠面板宽度可调 hook：宽度存 localStorage 跨会话保留 + onPointerDownResize 拖左缘手柄改宽（供 NodeInspector）
  hooks/useCompositionField.ts 「受控 + 写外部 store」输入的 IME 兼容 + 300ms 防抖提交 hook（血泪注释见文件头）；useCompositionFieldControls 完整版另暴露 setValue（程序化写入：本地立即更新+立即提交，@ 引用插入 token 用）与 isComposing（组词态查询）；useCompositionField 即其 .field 薄封装（既有调用点零改动）
  hooks/useMentionMenu.ts      Prompt 节点 @ 菜单逻辑 hook：非组词态输入 @（前一字符非字母数字，防 email 误触）→ 记 anchor + collectMentionCandidates 算候选并打开；继续输入按显示名过滤（跨行/超长自动关）；↑↓/Enter/Tab/Esc（Esc stopPropagation 防 React Flow 取消选中）；select 时同身份复用已有 mention、新身份消歧命名，拼 nextText 后经 onInsert 回调落库；另暴露 anchorIndex（`@` 字符下标）供高亮层渲染光标测量锚点
  hooks/useMentionHover.ts     @ tag 悬停命中测试：药丸画在 Textarea **下方**的高亮层且 pointer-events-none，挂不了 onMouseEnter，故在 Textarea 上听 mousemove（rAF 合帧节流 + 150ms 延时才弹），遍历药丸的 getClientRects()（跨行 token 有多条）判包含 → findMentionResource 取资源快照；拖选/菜单打开/输入/滚动/失焦时清空
  hooks/useUpdateCheck.ts      检查更新：挂载时静默自查一次（有新版才提示，失败不打扰）+ check() 手动查（跳后端缓存）；仅桌面端生效（supported=false 时不发请求）；比较用前端 APP_VERSION
  hooks/useTrackedRect.ts      每帧重测一个屏幕矩形供 fixed 浮层定位（取整签名不变则不 setState）：矩形失效来源太多（画布平移缩放/节点拖动/Textarea 滚动/窗口 resize/面板开合），逐个监听易漏，故用 rAF 轮询（同 floating-ui autoUpdate({animationFrame:true}) 思路）；只在浮层挂载期间运行；可传 clipRef，锚点滚出该容器时返回 null 让浮层隐藏
  components/ui/               shadcn/ui vendored（不参与 lint/format，勿手改）
  components/gate/ReqFromGate.tsx 启动强制填写 req_from：设置已加载且全局署名为空时全屏阻断弹窗，填写保存后放行（已填则不出现）
  components/settings/SettingsDialog.tsx 设置面板：底部「关于」区显示当前版本 + 「检查更新」按钮（有新版则给 Release 下载链接；仅桌面端显示）；全局 req_from（署名）+ AIGC 生成端点 / 图片上传端点 / 音频上传端点 / **AIGC 历史查询接口**（留空则不启用「结果找回」）+ **接口协议下拉**（Responses API /responses · Chat Completions /chat/completions；放在地址**之上**——它决定地址被补什么后缀，端点 placeholder 随之变化；连接测试带上草稿协议、成功文案回显实际生效协议）/ Agent 接口地址（只填到 /v1）/ Agent API Key（password 框，**写入-only**：不回显、留空=保持已存值、placeholder 按 hasAgentApiKey 提示）/ Agent 模型名（**下拉**：候选 = 手动维护的「模型列表」(下方 textarea 每行一个、持久化) ∪ 端点 GET /models 动态获取(「获取模型列表」按钮，进 API 分区自动、用当前表单 endpoint/key 作 override 支持存前预览)，经 mergeModelOptions 去重排序 + 已选值置顶；**不支持 /models 的网关靠手动列表也能得到多选下拉**）+ 保存
  components/presets/PromptPresetsDialog.tsx  Prompt 预设管理弹窗（顶栏「预设」按钮打开）：顶部新增/编辑表单（含「常用/System」分组选择）+ 下方按两组列出预设（编辑/删除）；预设为全局共享库，Prompt 节点可下拉一键选用或「存为预设」
  components/agent/AgentChatPanel.tsx    画布 Agent 聊天面板（AgentChatPanel：工作区右侧 360px 固定栏，头部收起按钮 + 消息气泡（用户右/助手左，执行反馈与失败信息小字）+ 输入区（Enter 发送、Shift+Enter 换行、输入法组词不误发）；AgentChatToggle：收起时画布右下角悬浮 Bot 按钮）
  components/theme/ThemeToggle.tsx 主题切换下拉（浅色/深色/跟随系统三选一；首页头部与工作区顶栏共用，图标随实际明暗变化）
  components/home/             HomePage（宫格/列表 + 新建；**置顶项目与其他项目分成两个独立容器**渲染——「置顶」区在上、Separator 分隔、「其他项目」区在下，故两者不同行；无置顶项目时不显示分区标题，布局与启用前一致）、ProjectCard（⋯ 菜单：置顶/取消置顶 · 重命名 · 删除；置顶项目带 Pin 图标——列表在名字前、宫格在左上角）
  components/workspace/ProjectWorkspace.tsx  顶栏 WorkspaceHeader + 画布 + Agent 面板（**无侧栏**）；外层 flex-col：顶栏通栏 + 其下 flex-row = 画布区（relative flex-1，内含 NodeInspector/AgentChatToggle 绝对定位）+ AgentChatPanel 固定宽靠右（NodeInspector 吸附画布区右缘，与聊天面板并排不重叠）；未 loaded 前不跳首页
  components/workspace/WorkspaceHeader.tsx   工作区顶栏：左=**AppLogo（品牌 + 版本号，纯展示不可点）** + **新版本提醒**（useUpdateCheck，仅桌面端且查到更高版本时出现的小徽标，点击去 Release 下载）+ 首页按钮（回首页的唯一入口）+ **可点击改名的项目名**（单击编辑，Enter 提交 / Esc 取消）；右=req_from 邮箱前缀 badge(点开设置 + 退出) · **预设按钮**(开 PromptPresetsDialog) · 主题切换 · 设置按钮(开 SettingsDialog)
  components/workspace/AppLogo.tsx            品牌标记：流程图 favicon SVG + APP_NAME + APP_VERSION（工作区顶栏使用，纯展示不可点——回首页交给顶栏的「首页」按钮）
  components/canvas/
    FlowCanvas.tsx             React Flow 封装；连线默认 bezier 曲线（default，旧 straight/smoothstep 在**渲染期**归一成曲线，不改库里的值）；**连线默认态一律是安静的淡灰细线**，类型色经内联 CSS 变量 --edge-color 下发（照 NodeHandle 的 --handle-color 写法），仅悬停 / 与「已选中节点」相连（edge-active）/ 边自身被选中时才由 CSS 取用并加粗——渲染期按 node.selected 派生 edge-active，不入库；纯颜色变化无动画；样式与「边恒在节点之下」的层叠规则都在 index.css；边另设 interactionWidth:12 收窄隐形命中带；colorMode 跟随主题（useThemeStore.resolved，画布底纹/控制按钮/缩略图随暗色）；onDragOver/onDrop 接桌面拖入文件（按 MIME 分图像/音频/视频，screenToFlowPosition 定位，**一律在落点建素材节点**并经 /api/upload 上传写回 URL（视频走媒体端点）——不再按落点区分「拖到节点上追加」；**不再接受节点类型的拖拽载荷**——建生成类节点只剩右键菜单与端点拉线两条路）；onPaneContextMenu 出加节点菜单，onNodeContextMenu/onSelectionContextMenu 在选中多节点时出「选中操作」菜单（分组/整理/取消分组，两菜单互斥）；**鼠标/触控板双操作模式**（鼠标=滚轮缩放；触控板=panOnScroll 双指平移 + zoomOnPinch 捏合缩放，偏好存 localStorage openflow-trackpad，默认鼠标模式，由 ZoomSlider 按钮切换）
    CanvasContextMenu.tsx      画布空白右键菜单：分组节点清单，点选即在落点加节点（含拉线松开在空白处的建节点+连线、批量连线按钮拖到空白处的建节点+批量连线）；挂载后按**实测尺寸**再夹一次位置（父级只按经验值预夹，落点靠下会被裁掉一截）
    MultiConnectHandle.tsx     批量连线按钮：框选 **≥2 个资源节点**时浮在选区包围盒中心（子节点坐标先转绝对再算盒；位置由 useViewport 把 flow 坐标换算成画布面坐标，故必须渲染在 <ReactFlow> 内部），从它拖出即可把选中资源**一并**连到目标——落点用 elementFromPoint 反查：优先精确落到的输入端点，否则落到的节点卡片走统一资源端点 res，都不是则回调 FlowCanvas 在该处弹建节点菜单（选完新建节点并全连上）。**不是 React Flow 的 handle**（起点不在任何节点上）故自绘拖拽：pointerdown 必须 stopPropagation（否则画布当成点空白清掉选中态）+ 预览线画在 portal 到 body 的 fixed SVG 上并 pointer-events-none（否则挡住落点判定），线色在选中资源同类型时取类型色
    SelectionContextMenu.tsx   选中节点右键菜单：分组 / 整理（网格排列）/ 取消分组（按选中情况显隐；≥2 非容器节点才可分组/整理，选中/点中容器才可取消分组）
    ZoomSlider.tsx             左下角缩放面板：−/滑块/+/百分比复位/适配视图 + 鼠标/触控板模式切换（图标随模式换 Mouse/Touchpad）与网格吸附、缩略图开关（偏好由 FlowCanvas 存 localStorage）
    DownloadDialog.tsx         下载/重命名对话框（受控 open + DownloadTarget）：用户只改文件名、扩展名由后端按响应类型自动补；triggerDownload 经 /api/download 同源代理触发浏览器下载（供图像/视频结果卡片）
    nodes/PromptNode.tsx       Prompt 节点（Card + Textarea）：左侧 target Handle 输入 + 右侧 source Handle 输出（左入右出，天蓝）；上游 Prompt 文本可连入，经 collectUpstreamPrompt 递归并入下游（Prompt 链）；卡片可从 **Prompt 预设**库一键选用（写入文本框，同步清空 mentions）或把当前文本「存为预设」(分常用/System，usePromptPresetStore)；文本框输入 **@** 弹 MentionMenu 引用下游连线资源（useMentionMenu + useCompositionFieldControls.setValue 插入 token 绕开防抖；提交文本时 pruneMentions 清理失效映射）；**@ tag 原子删除**：光标紧随已登记 token 时退格整体删掉该 token（mentionTokenEndingAt 判定，未登记的 @[xxx]/带修饰键删词/IME 组词/有选区一律放行原生）；**悬停 tag 弹预览**（useMentionHover 在 Textarea 上做几何命中测试 → MentionPreview）
    nodes/MentionMenu.tsx      @ 引用浮层三件套（均自绘，**不用 Radix Popover/DropdownMenu——其 pointerdown 会被 React Flow 节点拖拽吞掉**，同预设弹窗的坑）：MentionMenu（候选菜单：**createPortal 到 body + fixed**，跟随光标定位、空间不足自动向上翻转、40px 缩略图、↑↓ 时选中项 scrollIntoView；**必须 portal**——节点卡片 shadcn Card 带 overflow-hidden 会裁掉浮层，且 React Flow 给每个节点 div 同时加 transform+z-index 形成独立层叠上下文，节点内 z-50 盖不住邻居节点；portal 后 React 合成事件仍沿 React 树冒泡到 NodeWrapper，故根元素 stopPropagation 掉 contextmenu/click；nodrag/nowheel 已非必需仅作防御） / MentionHighlights（@ tag 高亮层内容：按 token 切片画底色药丸 + data-mention-name 供命中测试，整体文字透明——真实文字由上层 Textarea 绘制；菜单打开时把 `@` 字符单独包一层无样式 span 作光标测量锚点——**包住真实字符而非插零宽 span**，空 inline 元素 rect 会塌成 0） / MentionPreview（tag 悬停预览：同样 portal+fixed，**必须 pointer-events-none**否则会让底层 mousemove 断流形成显示/隐藏闪烁循环）；另有 ResourceThumb 私有组件：图加载失败回落成类型图标（调用方传 key={url} 重置）
    nodes/ImageNode.tsx        图像生成节点：输入图**来自连线到统一资源端点 res 的资源**（上游图像素材/生成结果可连多条；上游 Prompt 里 @ 到图像则只发被 @ 的、没 @ 全发；右侧 Inspector 的 ImageInput 只读预览实发列表，不再上传/手填 URL，无 Add Input 按钮）/按模型(Image 2 走尺寸/质量/张数；Nano Banana 走 version/宽高比/尺寸)；运行收集上游 Prompt 文本→createImageTaskApi 建任务→pollTask 轮询→展示结果图；带 taskId 载入时 useEffect 重连轮询（关页面不丢结果；重连/Agent 触发路径失败 **silent 不弹 alert** 只节点内联报错，点击运行路径失败仍弹窗；**可恢复的失败（task.recoverable）也不弹窗**——面板里就能重拉）；失败时**保留 taskId**（清掉的话底部面板就没法凭它去历史重拉），错误区用 TaskFailurePanel；Handle 左进右出（req_from 署名走全局设置，节点不再单设）
    nodes/SeedanceNode.tsx     视频生成节点（seedance / kling / MiniMax-H3 共用此组件，差异全在能力表与请求构造里）：输入资源**来自连线到统一资源端点 res 的图/音/视源**（@ 筛选语义同图像节点；首尾帧变体另保留 First/Last 两个专用图像端点定图序，res 收音频等；切换变体后 useUpdateNodeInternals 重测端点；无 Add Input 按钮；Inspector 只有 version/mode/分辨率/时长参数，无上传/手填 UI）；运行时输入图 = 上游图像(连线)、音频 = 上游音频素材(连线) 作 audios→createVideoTaskApi 建任务→pollTask 轮询→<video> 展示；带 taskId 载入时 useEffect 重连轮询；失败处理同 ImageNode（可恢复失败不弹窗、保留 taskId、错误区用 TaskFailurePanel）；Handle 左进右出——**右侧 Video 输出端点为视频源（玫红）**（sourceKind(video)='video'，结果视频可经连线作下游视频节点的参考视频）；req_from 署名走全局设置，节点不再单设
    nodes/PodcastNode.tsx      播客音频节点（火山 TTS）：内置脚本 Textarea（useCompositionField 防抖+IME）+ 运行 →createPodcastTaskApi 建任务→pollTask 轮询→<audio> 播放 + 下载（同源 URL 直接 <a download>）+ 计费字数展示（result[1]）；带 taskId 载入时重连轮询；NodeResizer 可调大小；终端节点无 handle（结果是相对 URL，发不去外部网关故不作下游输入源；addConnectedNode 对 podcast 特判不连线）
    nodes/SplitterNode.tsx     脚本切割节点：整篇脚本原文 Textarea + 双角色名 + **语速档位下拉**（原生 select——Radix 下拉在 RF 节点内打不开；选项取 nodeCatalog.STORYBOARD_SPEED_OPTIONS，值存 data.charsPerSecond，读取经 normalizeSplitSpeed 归一），点「切割」buildItems(script,roles,语速) 切段后经 splitScriptToStoryboard 写进/新建下游分镜节点（反馈「已切 N 段 → 新建/更新」）；右侧唯一输出端点连分镜节点 segments 口（文本粉）；NodeResizer 可调
    nodes/StoryboardNode.tsx   脚本分镜节点（**Excel 式 <table>**，表头 序号/发言人/脚本/时长/生成/prompt，sticky 表头 + 列边框）：序号列（可编辑，失焦/回车提交=**把本行挪到该位置**；running 中禁用——worker 按下标写回会错位）/ 发言人列（**原生 <select>** 切角色——Radix 下拉在 RF 节点内打不开）/ 脚本列（StoryboardRow 子组件内 useCompositionField 可编辑，改完 estimateSegmentDuration 自动重估时长）/ 时长列（本地承接失焦夹 4~15 提交；「渲染期对比上次 prop」派生同步，effect 里 setState 会被 lint 拦）/ 生成列=状态 + 单段生成/重跑 ▷ / prompt 列只读可滚动（select-text 可手动选中复制）；工具栏「复制表格」（itemsToTsv → navigator.clipboard）与「从 Excel 粘贴」（粘贴区 onPaste 拦截 → parseItemsTsv 导入重建表格）；表格三入口=上游切割节点 / 折叠的「粘贴脚本切分」区 / Excel 粘贴导入；「生成」对全表**逐段并发调 /api/agent/expand**（worker 池限并发 STORYBOARD_CONCURRENCY=3，{{duration}} 前端替换、{{line}}=「角色名: 段文本」后端替换，写入全走 patchStoryboardItem 防并发覆盖，AbortController 卸载中止 + 被中止段清扫回 idle）；「落成节点」把 done 段经 addStoryboardShots 落成 N 组「Prompt→Seedance(reference)」节点对（写入 C 列时长，不自动运行）；左侧 5 端点（segments 分镜表 + role-image-N×2 + role-audio-N×2）**按角色分组**：槽位 0=分镜表 / 1·2=角色A 参考图·音色 / **3 空出** / 4·5=角色B 参考图·音色（组内 32px、组间 64px，槽位常量 SLOT_* 在组件顶部）；NodeResizer 可调；无输出端点（addConnectedNode 特判同 podcast 不连线）
    nodes/AssetNode.tsx        素材节点（桌面拖入）：图像素材显示缩略图 / 音频素材显示 <audio> / 视频素材显示 <video>；上传中骨架、失败内联；仅右侧 source Handle（纯源，图像绿/音频蓝/视频玫红，连下游作输入）
    nodes/GroupNode.tsx        分组容器节点：半透明虚线框包住子节点（子节点 parentId 指向它、渲染在其上方，拖框子节点跟随）；顶部工具条改名 + 取消分组；NodeResizer 可调大小；无连接点
    nodes/NodeHeader.tsx       各节点共用卡片头部：图标 + 名称（**双击重命名**：内联 input 编辑 data.label，Enter/blur 提交、Esc 取消、IME 守卫、trim 空放弃；双击 stopPropagation 防触发画布双击缩放）+ 可选 subtitle 小字副标题（Image/Video 的模型名、素材的文件名——标题让位给 label 后原信息降级到此）+ 复制 + 删除按钮（默认隐藏，hover/选中时显示）；复制按钮调 duplicateNode()——在原节点正右侧（让开整宽 + 间距，不重叠）生成副本，保留参数与结果快照与**输入连线**、清 taskId/运行态，选中态转到副本（group 容器无此头部故不可复制）
    nodes/TaskFailurePanel.tsx 生成失败时节点底部的错误区（图像/视频节点共用）：红字文案 + 三条自救路径——「重新拉取结果」(refetchTaskApi 再去 AIGC 历史认领一次，仅 recoverable 时出现)、「手动填入 URL」(从别处复制回来直接写进 data.result，零外部依赖)、「上游响应」(展开 getTaskApi 拿 rawResponse + 复制，供排查/反馈)。**rawResponse 按需现拉不进节点 data**，免得原始响应随项目 JSON 一起膨胀
    nodes/NodeHandle.tsx       统一端点：环形连接点 + 外侧标签；type(target/source) + index(竖向槽位) + tone(粉/绿/蓝) + required(必填端点实心)
    nodes/handleLayout.ts      端点竖向布局常量/计算：HANDLE_TOP_START=48 + HANDLE_GAP=32 + handleTop(index)/handleStyle(index)
    nodes/index.ts             nodeTypes 注册表（prompt → PromptNode / image → ImageNode / video → SeedanceNode / asset → AssetNode / group → GroupNode / splitter → SplitterNode / storyboard → StoryboardNode）
  components/inspector/        右侧节点参数面板（Inspector）：节点卡片只显示结果，参数都在此编辑
    NodeInspector.tsx          仅在「恰好选中一个 image/video/podcast 节点」时出现；按类型装配子面板（image → ImageInput + ImageParams；video → VideoInput + VideoParams；podcast → PodcastParams）+ 置底「请求」预览（buildXxxRequest 的实发请求体，JSON / 表格 两视图可切换、偏好存 localStorage）；宽度经 useResizableWidth 可调
    ImageInput.tsx             输入图**只读预览**（图像节点用）：直接展示 buildImageRequest 的实发 image_list（@ 筛选后 + 旧手填 URL 殿后），与请求预览同源；无输入图时提示拖图建素材节点连线 + @ 指定（不再上传/手填 URL）
    VideoInput.tsx             输入资源**只读预览**（视频节点用，ImageInput 的视频版）：视频吃图/音/视三类且全靠连线喂，此前面板上完全看不见，故三类分区一并展示 buildVideoRequest 的实发 images/audios/videos（@ 筛选、连线序、frames 只取 First/Last 前 2 张等语义都已在其中算好，与请求预览同源）；frames 变体图序即端点语义，角标标「首帧/尾帧」而非序号；三类皆空时出空态提示（按变体分文案）
    ResourcePreview.tsx        上两者共用的只读展示件：ImageThumbs(3 列方格 + 1 基序号角标，对应 prompt 里的 <<<image_N>>>；加载失败只隐藏图元不塌布局) / VideoThumbs(2 列，preload="metadata" 只拉首帧——面板里可能挂好几个参考视频，全量预加载白吃带宽，点开新标签页看完整内容) / AudioRows(整宽可试听) / PreviewSection(分区小标题) / PreviewEmpty(虚线空态框)
    ImageParams.tsx            图像节点参数控件：按模型分两套（Image 2 走 size/quality/n；Nano Banana 走 version/aspectRatio/imageSize）；旧尺寸回退到受支持默认
    VideoParams.tsx            视频节点参数控件（三个模型共用一份，全按 videoModelSpec 渲染）：version + 分辨率(可灵无此项)/宽高比(2.5 首尾帧锁死 adaptive 并标注原因)/生成质量(可灵 std·pro) + 时长滑块(范围随 spec；支持自动时长的版本多一个「自动时长」开关，开启即下发 -1) + 各模型特有开关（生成音频 / 生成音效 / AIGC 水印）+ 可灵多镜头分区；**展示值用与实发同一套归一化函数算**，杜绝「面板选着 1080p 实际发 720p」
    KlingShotsEditor.tsx       可灵多镜头（config.multi_shot）分镜编辑器：开关 + 最多 6 段（每段 prompt(useCompositionField 防 IME) + 时长，可增删）+ 「时长合计 vs 总时长」配平提示（**只提示不强改**——加一段时和值必然先对不上，替用户挪时长更烦人）；开启后顶层 prompt 不下发，改发 multi_prompt
    ToggleRow.tsx              Inspector 通用开关行（整行可点 + 右侧「开/关」）；仓库没引 shadcn Switch，播客与视频的布尔可调项共用这一件
    PodcastParams.tsx          播客节点参数：两个角色（角色名 + 火山音色 ID，从火山控制台音色库复制）+ 音频参数（采样率下拉 / 语速 / 音量 / 音调 / 句间停顿滑块）+ 文本处理开关（过滤括号内容——会连 [轻笑] 表演指令一起滤、过滤 Markdown、过滤 Emoji、朗读语种下拉）+ AIGC 水印（aigc_watermark 生成标识开关——逐句合成每句结尾都有；aigc_metadata meta 隐式水印开关 + 四个元信息小输入，开启时每句按 wav 请求）；语音指令（context_texts）UI 已隐藏、字段保留
    components/model/ModelCapabilityBadges.tsx  模型能力小图标：读 modelCapabilities(name) 只渲染推断支持的能力（思考=Brain/图像=Eye/音频=AudioLines/视频=Video，各带中文 tooltip），供设置面板 Agent 模型名下拉的选项使用（Radix SelectValue 会连带在 trigger 显示已选模型的图标）
apps/desktop/
  src/main.ts                  Electron 主进程：dataDir=userData → startServer(内嵌后端)；生产固定端口 42617（保证 localStorage origin 稳定，主题/homeView 偏好可跨启动保留；被占用才回退随机端口）+ 托管 SPA 后 loadURL(localhost)，开发连 VITE_DEV_SERVER_URL(5173)；窗口 backgroundColor 跟随 nativeTheme 明暗（防暗色用户启动白闪）；含 OPENFLOW_SELFTEST 无界面自检分支
  src/preload.ts               预加载：contextIsolation，仅暴露 window.openflow.desktop 标记（渲染进程只用 fetch 访问本地 /api）
  src/external-links.ts        外链归属判定 classifyLink(url, appOrigin) → internal/external/ignore：主进程据此把站外 http(s) 交给 shell.openExternal（**新版本提醒、结果图/视频等 target="_blank" 一律走系统默认浏览器**，Electron 默认会开成没有地址栏的新 BrowserWindow）；非 http(s) 一律丢弃（不让页面内容驱动系统去开本地文件/自定义协议）；接线在 main.ts 的 web-contents-created（setWindowOpenHandler + will-navigate 双拦）
  src/external-links.test.ts   上述判定的 node:test 单测（`pnpm --filter @openflow/desktop test`，走 node --experimental-strip-types 直接跑 TS，不引测试依赖）
  scripts/build.mjs            esbuild 把 main/preload + @openflow/server 打成 CJS(dist-electron/*.cjs，better-sqlite3/electron 外部化) + 拷 apps/web/dist → dist-electron/web
  scripts/sqlite-abi.mjs       在 Node/Electron ABI 间切 better-sqlite3（node=prebuild-install / electron=electron-rebuild / win=prebuild-install 拉 win32-x64+Electron ABI 预编译产物，供 mac 交叉打包 win）
  electron-builder.yml         打包配置：asar + better-sqlite3 解包(asarUnpack)；mac dmg/zip(arm64+x64,identity:null 未签名)、win nsis(x64 未签名)；`toolsets`(nsis 1.2.1 / winCodeSign 1.1.0) 指定含 arm64 原生二进制的 win 工具链——见「桌面端打包」的 Apple Silicon 说明
```

## 技术约束

- **框架 / 构建**：React 19 + Vite + TypeScript（strict）；pnpm workspaces。
- **后端**：Hono + `@hono/node-server`，`tsx watch` 跑（免构建）；端口 8787。Vite dev 代理 `/api` → 8787（`apps/web/vite.config.ts`）。
- **数据库**：SQLite（`better-sqlite3`，单文件；开发在 `apps/server/data/openflow.db`，桌面端在 `userData` via env `OPENFLOW_DATA_DIR`）。`projects` 表 nodes/edges 存 JSON + `pinned`(置顶 0|1)；`settings` 单行存 `default_req_from`(全局署名) + `aigc_endpoint`/`upload_endpoint`/`upload_media_endpoint`(可配置端点)；`tasks` 表存异步生成任务(状态/参数/结果 JSON)；`prompt_presets` 表存全局 Prompt 预设(title/content/category)。原生模块装不上时回退 Node 内置 `node:sqlite`。
- **数据流**：前端无 localStorage 持久化（仅 homeView / 主题偏好 + 迁移标记用 localStorage）。项目数据走 `/api/projects`；画布高频编辑防抖 PUT。全局 req_from 走 `/api/settings`。
- **画布 Agent**：`POST /api/agent/chat` 为**同步**接口（LLM 在请求内返回，前端 request 封装无超时、后端对上游 120s 超时）；LLM 端点/Key/模型/**协议** = **设置里 `agentEndpoint`/`agentApiKey`/`agentModel`/`agentApiStyle` 优先，为空回退 env `AGENT_ENDPOINT`/`AGENT_API_KEY`/`AGENT_MODEL`/`AGENT_API_STYLE`，端点或模型均无则 400 提示去设置（无内置默认）；协议均无则默认 `responses`**。**双协议**：`agentApiStyle='responses'` 发 `POST {base}/responses`（instructions + input，读 output[]），`'chat'` 发 `POST {base}/chat/completions`（messages，读 choices）——差异集中在 `apps/server/src/llm.ts`，`agent.ts` 只管业务语义；`agentApiKey` 密钥**写入-only**（GET /api/settings 只回 hasAgentApiKey 标记，不回明文）；不要求 req_from（那是 AIGC 网关的署名约定，生图动作仍由既有 `/api/aigc` 链路强制校验）。Agent 建的节点/连线与手动创建完全同构：可手动改 Prompt 后点节点「生成」重跑（collectUpstreamPrompt 正常采集）。**模型选择动态化**：设置里的 Agent 模型名为下拉，候选 = **手动维护的模型列表**（`settings.agentModelList`，持久，在设置面板 textarea 每行一个）**∪ 端点 `GET /models` 动态获取**（经后端 `POST /api/agent/models` 代理注入密钥、绕 CORS，存 `useSettingsStore.agentModels` 会话态），经 `mergeModelOptions` 去重排序 + 已选值置顶。**支持 /models 的网关自动填充下拉、不支持的靠手动列表填充**，两种都得多选下拉；并集为空才回退手填。
- **生成调用**：图像 `/api/aigc`、视频 `/api/video` 现为**异步任务**——建任务行、立刻返回 `taskId`，AIGC 由 `task-store` 进程内 runner 后台跑（不阻塞响应）；前端把 `taskId` 存进节点 data（随项目防抖 PUT 落库），凭 `pollTask` 轮询 `/api/tasks/:id` 至终态。**关页面/刷新不丢结果**：重开后节点带 `taskId` 载入 → useEffect 重连轮询拿回结果；进程重启时残留 running 任务由 `reconcileInterruptedTasks()` 处理（配了历史查询接口就重新挂上复查续跑，否则标 failed）。**上游没带回结果 URL 时不判死**：`sendOrRecover` 转去 `aigc-history.ts` 按 `request_id` / 请求指纹在网关历史里认领结果（上游明确失败则直接报 `result.error.message` 里的真实原因，不做无谓复查）。文件上传 `/api/upload`（图片/音频按 `kind` 分流）仍是同步代理转发。以上均经后端绕 CORS；req_from 由后端从全局设置注入，**为空则直接返回 400 拒发、无兜底默认（`AIGC_REQ_FROM` 已不再使用）**。**端点地址优先取全局设置里的 `aigcEndpoint`/`uploadEndpoint`/`uploadMediaEndpoint`，为空才回退 env（`AIGC_ENDPOINT`/`UPLOAD_ENDPOINT`/`UPLOAD_MEDIA_ENDPOINT`）再回退内置默认**——便于打包分发后由用户自填，不写死内网 IP。
- **启动门槛**：`ReqFromGate` 在设置加载后若 req_from 为空则全屏阻断，必须填写署名才放行。
- **路由**：`react-router-dom` `HashRouter`。
- **画布**：React Flow（`@xyflow/react`），节点是普通 React 组件；连线 bezier 曲线（默认淡灰细线不抢注意力；悬停 / 选中节点相连 / 边被选中时才上类型色并加粗，无动画）。**连线恒在节点之下**：React Flow 会给「任一端在分组容器里」的边算出 zIndex ≥ 1 从而盖住未分组节点（z=0）并吞掉点击，故 index.css 给 `.react-flow__edges` 一个 z-index 让边容器自成层叠上下文压住这些值。
- **UI**：shadcn/ui（Tailwind v4）。新增组件 `pnpm dlx shadcn@latest add <name>`（在 apps/web 内）。**深色模式**：class 策略（`.dark` 挂 `<html>`，Tailwind `@custom-variant dark`），组件一律用语义 token（bg-background/text-muted-foreground 等）自动适配，勿写死明色/暗色；主题偏好经 `useThemeStore` 存 localStorage，`index.html` 内联脚本首帧前挂类防白闪；`index.css` 末尾 `.dark { color-scheme: dark }` 让原生控件（滚动条 / `<audio>` / `<video>`）跟随。
- **路径别名**：`@/*` → `apps/web/src/*`。
- **共享类型**：跨前后端的纯数据契约放 `packages/shared`；后端不引 `@xyflow/react`，nodes/edges 当不透明 JSON。

## 桌面端打包（Electron，`apps/desktop`）

- **原理**：Electron 主进程直接内嵌 `@openflow/server` 的 `startServer()`（同一进程跑 Hono），生产环境让它顺带在根路径托管 `apps/web` 构建产物，窗口 `loadURL(http://localhost:<固定端口 42617，被占才回退随机>)`。→ 渲染进程 origin 即该 localhost，**现有前端相对 `/api` 调用零改动可用**；固定端口保证 localStorage origin 跨启动稳定（主题 / homeView 等 UI 偏好不丢）。数据库落 `app.getPath('userData')`（经 `OPENFLOW_DATA_DIR` 注入）。
- **构建链**：esbuild 把 `main.ts`/`preload.ts` + 内联的 `@openflow/server` 打成 CJS（`.cjs`，`electron` 与 `better-sqlite3` 外部化）；`apps/web` 仍用自身 Vite 构建，产物拷进 `dist-electron/web`。`@openflow/server` 在 desktop 里是 **devDependency**（打包时被 esbuild 内联，运行时不需要）；唯一真正的运行时原生依赖是 `better-sqlite3`。
- **⚠️ 原生模块 ABI 冲突（重要）**：`better-sqlite3` 是编译过的原生模块，Node 与 Electron 的 ABI 不同，而 pnpm 让二者共用同一物理副本，**一次只能是一种 ABI**。故：
  - `pnpm dev:all` / `pnpm server`（普通 Node）需 **Node ABI**；`electron .` 与打包需 **Electron ABI**。
  - `scripts/sqlite-abi.mjs` 负责切换：`rebuild:node`（prebuild-install）/ `rebuild:electron`（electron-rebuild）/ `rebuild:win`（prebuild-install 拉 win32-x64+Electron ABI 预编译产物）。
  - **⚠️ 交叉打包 win（在 mac 上）**：原生模块无法在 mac 上为 Windows 编译，electron-builder 对「异平台」目标**不会重建**（会把 node_modules 里当前那份 `.node` 原样拷进包）。故 `dist:win`/`dist:all` 的 win 步骤先 `rebuild:win` 把 win32-x64+Electron ABI 的 `better_sqlite3.node` 覆盖进 node_modules，再 `electron-builder --win -c.npmRebuild=false` 原样打包——**否则包里会混进 mac 的 `.node`，Windows 上加载即崩、内嵌服务起不来、窗口不出现（进程在但无界面）**。彻底可靠的方式仍是在 Windows/CI 上原生构建。
  - `dist:mac`/`dist:win`/`dist:all` **打包结束会自动 `rebuild:node` 还原**（打好的 app 已自带对应 ABI 副本），故打包不破坏 `pnpm dev:all`；脚本用 `...; code=$?; pnpm run rebuild:node; exit $code` 而非 `&&`，**打包失败也一定还原 ABI**（否则失败后 node_modules 卡在 win ABI，`pnpm dev:all` 起不来）；`pnpm --filter @openflow/desktop dev/start` 会先切 Electron ABI，用完想跑普通 Node 服务需手动 `pnpm --filter @openflow/desktop rebuild:node`。
- **⚠️ Apple Silicon 上打 win 包需 electron-builder ≥ 26 + 新工具链**：electron-builder 25 在 mac 上改 exe 图标/版本信息走 `wine + rcedit.exe`，其 NSIS 的 `makensis` 也是 x86_64 二进制 —— **未装 Rosetta 2 的 arm64 mac 会 `bad CPU type in executable` / `spawn Unknown system error -86`**。修法：升到 electron-builder **26**（exe 资源编辑改用纯 JS 的 `resedit`，彻底不需要 Wine）+ 在 `electron-builder.yml` 配 `toolsets: { nsis: 1.2.1, winCodeSign: 1.1.0 }`（新工具链包带 arm64 原生 `makensis`/`osslsigncode`；默认的 `0.0.0` 仍是旧 x86_64 版）。另 electron-builder 26 会带进 `electron-winstaller`，我们只出 nsis，故在 `pnpm-workspace.yaml` 的 `allowBuilds` 里置 `false` 拒绝其 postinstall。
- **CI 发版（`.github/workflows/release.yml`）**：push `v*` tag → `prepare`(版本三方校验) → `build` 矩阵(**macos-latest / windows-latest 各自原生构建**) → `release`(统一建 GitHub Release)。CI 上**不走** `dist:mac`/`dist:win`（那两个含 POSIX `$?` 语法 + `rebuild:node` 还原，是本地专用），而是 `web build` → `desktop bundle` → `electron-builder --mac|--win --publish never`。**⚠️ `--publish never` 必须带**：electron-builder 26 检测到 CI 会隐式把发布策略设成 `onTagOrDraft` 并读 `GH_TOKEN`，两个 build job 会抢建同一个 Release。因各平台原生构建，CI 上**不需要** `rebuild:win` 交叉打包与 `toolsets` 的 Apple Silicon workaround（本地仍需要，故保留）。手动触发(workflow_dispatch)在分支上只出制品不发 Release，在 tag 上则进入发布模式（供重跑）。版本三处（tag / `apps/desktop/package.json` / `appMeta.ts`）由 `scripts/check-version.mjs` 校验一致，`pnpm check:version` 可本地自查。
- **分发**：产物命名 `OpenFlow-<ver>-mac-arm64.dmg` / `-mac-x64.dmg` / `-win-x64-setup.exe`（显式配 `artifactName`——默认模板会省掉默认架构 x64 的后缀，Intel 用户看不出该下哪个）。当前 mac(arm64 + x64/Intel dmg/zip，各自内置对应 arch 原生模块) / win(x64 nsis) 均 **未签名**（内部自用）；mac 首次打开需右键「打开」绕过 Gatekeeper，win 点「仍要运行」绕过 SmartScreen。产物在 `apps/desktop/release/`（gitignore；x64 dmg 无 arch 后缀 `OpenFlow-<ver>.dmg`，arm64 为 `-arm64.dmg`）。正式对外分发需另配 Apple Developer ID 公证 + Windows 代码签名证书。
- **端点分发友好**：AIGC/上传/历史查询地址**不内置任何默认值**（`provider.ts` 的 env 回退为空串，两者都空时 `resolveEndpoint` 抛「请先在设置中填写」），一律由设置面板填（存后端 settings）；打包发给不同网络的人也能自行改地址，仓库公开时也不会带出内网拓扑。
- **pnpm 注意**：`@electron/rebuild` 用 git 引用 `@electron/node-gyp`，`pnpm-workspace.yaml` 里用 `overrides` 覆盖成 npm 发布版绕开 exotic-subdep 拦截；`electron` 的 postinstall 需在 `allowBuilds` 放行。

## Git 工作流（分支与推送规则）

- **双分支模型**：`dev` = 开发/集成分支，**一切日常操作的基点**；`main` = 发布分支，**只接受来自 `dev` 的 PR 合并**——禁止直接 push main，也不接受其他分支直接 PR 进 main。
- **改动流程**：从最新 `dev` 拉临时分支（`feature/<名>` / `fix/<名>` / `docs/<名>`）→ 提交 → **PR 合并回 `dev`**；累积到发布点后再发起 **`dev` → `main` 的发布 PR**。
- 发布 PR 合并后：把 `dev` 快进同步到 main 的合并提交（保持两支一致）；已合并的临时分支**随手删除**（不留长期分支，建议开启 GitHub 的 Automatically delete head branches）。
- 仓库为私有 + 免费套餐，**分支保护规则不生效**（已建 Ruleset 处于待生效状态）——以上规则靠约定执行，人和 AI 工具都必须遵守。
- 提交信息沿用现有风格（`feat: ✨` / `fix: 🐛` / `docs: 📝` / `chore: 🔖` 等 gitmoji 前缀）；提交前跑「编码规范」末条的 lint/typecheck/build。
- 若 `main` 意外被直接推送导致与 `dev` 分叉：先 `git merge-tree --write-tree` 干跑查冲突，尽快把 main 合回 dev，不要放任分叉变大。

## 编码规范

- 组件文件 PascalCase，函数组件具名导出。
- 新增节点类型需同步更新 `apps/web/src/lib/types.ts`、`nodes/index.ts`、`createNode()`(store)、`lib/nodeMenu.ts` 的 `NODE_GROUPS`（画布右键新建 + 端点拉线建节点共用）；图像/视频类的预置模型在 `lib/nodeCatalog.ts`。（例外：`asset` 素材节点不走 `NODE_GROUPS`/`createNode`，由 `FlowCanvas` 拖拽经 `addAssetNode()` 创建；`group` 分组容器也不走 `NODE_GROUPS`/`createNode`，由 `groupSelectedNodes()` action 按选中节点包围盒创建，故未加入 `FlowNodeType`/`NODE_GROUPS`。）
- 节点内可交互元素加 `nodrag` class。
- 不手改 `apps/web/src/components/ui/*`、`src/hooks/use-mobile.ts` 与 `index.css` 的 shadcn 主题块（生成内容，已在 eslint globalIgnores 排除）。
- 改后端 SQLite 表结构时注意已有数据兼容。
- 提交前确保根 `pnpm -r lint`、`pnpm -r typecheck`、`pnpm -r build` 通过。

## 后续可扩展（当前未做）
- 多用户 + 鉴权；流式输出（SSE）。
- 按连线拓扑顺序自动编排执行（把上游输出喂给下游）。
- 导入 / 导出工作流 JSON、撤销重做、更多节点类型。
